{
  "name": "@cbfish/design",
  "version": "0.0.1",
  "description": "CB Fish (Columbia Basin Fish & Wildlife Program) — an Astro brand spoke of the ESA Ecology hub (../ecology).",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && gh-pages -d dist --dotfiles",
    "handoff": "handoff http://localhost:4321/ --name home --out public/handoff"
  },
  "dependencies": {
    "@esa/ecology": "file:../ecology/packages/ecology",
    "@esa/tokens": "file:../ecology/packages/tokens",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@esa/handoff": "file:../ecology/packages/handoff",
    "gh-pages": "^6.3.0"
  }
}
